[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "code-review-agent"
version = "1.0.0"
description = "An OpenEnv environment for AI code reviews"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai"
]